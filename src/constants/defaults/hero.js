export const DEFAULT_HERO = {
  scrollLabel: 'Scroll down ↓',
  portraitUrl: '/hero/portrait.png',
};

/** Fallback public path when no portrait is uploaded in CMS */
export const HERO_PORTRAIT_FALLBACK = '/hero/portrait.png';
